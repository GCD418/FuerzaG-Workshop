@page "{id}"
@model FuerzaG.Pages.Services.EditModel
@{
    ViewData["Title"] = "Editar Servicio";
}

<h2 class="mb-3">Editar Servicio</h2>

<form method="post" class="card card-body shadow-sm">
    <div asp-validation-summary="ModelOnly" class="text-danger mb-2"></div>

   
    <input type="hidden" asp-for="EncryptedId" />
    <input type="hidden" asp-for="Id" />

    <div class="row g-3">
        <div class="col-md-6">
            <label asp-for="Name" class="form-label">Nombre del servicio</label>
            <input asp-for="Name" class="form-control" required minlength="3" maxlength="100" pattern="^[a-zA-Z\s]+$" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>

        <!-- Validación de Tipo -->
        <div class="col-md-6">
            <label asp-for="Type" class="form-label">Tipo</label>
            <input asp-for="Type" class="form-control" maxlength="50" required />
            <span asp-validation-for="Type" class="text-danger"></span>
        </div>

        <!-- Validación de Precio -->
        <div class="col-md-6">
            <label asp-for="Price" class="form-label">Precio</label>
            <input asp-for="Price" class="form-control" type="number" step="0.01" min="0" required />
            <span asp-validation-for="Price" class="text-danger"></span>
        </div>

        <!-- Validación de Descripción -->
        <div class="col-12">
            <label asp-for="Description" class="form-label">Descripción</label>
            <textarea asp-for="Description" class="form-control" rows="3" maxlength="500"></textarea>
            <span asp-validation-for="Description" class="text-danger"></span>
        </div>

        <!-- Activo -->
        <div class="col-12">
            <div class="form-check">
                <input asp-for="IsActive" class="form-check-input" />
                <label asp-for="IsActive" class="form-check-label">Activo</label>
            </div>
        </div>
    </div>

    <div class="mt-3">
        <button class="btn btn-primary" type="submit">Actualizar</button>
        <a asp-page="/Services/ServicePage" class="btn btn-outline-secondary">Cancelar</a>
    </div>
</form>
