@page "{id:int}"
@model FuerzaG.Pages.Owners.EditModel
<h2>Editar Dueño</h2>

<form method="post" class="card card-body">
  <div asp-validation-summary="ModelOnly" class="text-danger mb-2"></div>
  <input type="hidden" asp-for="Form.Id" />
  <!-- usa el mismo bloque de inputs que en Create, cambiando Form.XXX -->
  <partial name="_OwnerFields" model="Model.Form" />
  <!-- Si no creas el parcial, copia los inputs de Create y cambia "Form." -->
  <div class="mt-3">
    <button class="btn btn-primary" type="submit">Actualizar</button>
    <a asp-page="/OwnerPage" class="btn btn-outline-secondary">Cancelar</a>
  </div>
</form>
